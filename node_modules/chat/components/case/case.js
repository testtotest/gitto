// components/case/case.js
var app = getApp()
Component({
    /**
     * 组件的属性列表
     */
    properties: {
        showNum: {
            type:Number,
        },
        pageSize: Number,
        caseData: Array
    },

    // observers:{
    //     'pageSize': function(pageSize){
    //         if(!this.data.nodata) {
    //             wx.showLoading({
    //                 icon: "none"
    //             })
    //             setTimeout(() => this.getCasesDate(),300)//防止拿不到token
    //         }
    //     }
    // },

    /**
     * 组件的初始数据
     */
    data: {
        imgBaseUrl:app.globalData.imgUrl,
    },

    /**
     * 组件的方法列表
     */
    methods: {
        handleProduct: (e) => {
            var id = e.currentTarget.dataset.id
            wx.navigateTo({
              url: `/pages/casedetail/casedetail?id=${id}`,
            })
        }
    }
})