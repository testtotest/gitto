// components/productitem.js
const app = getApp()
Component({
    /**
     * 组件的属性列表
     */
    properties: {
        productData:Object,
        type: String //判断是搜索页面  还是 产品页面
    },

    /**
     * 组件的初始数据
     */
    data: {
        productData:{},
        type:"",
        imgBaseUrl:app.globalData.imgUrl
    },

    /**
     * 组件的方法列表
     */
    methods: {
        handleProduct:function(e){
            //  console.log('产品组件________id',e,e.currentTarget.dataset.id)
            //  console.log('这是错误的取值方式',e.target.dataset.id)
        
             this.triggerEvent('detailProdut', { id: e.currentTarget.dataset.id})
            }
    }
})
