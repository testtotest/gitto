<!-- <view>
	<input type="text"  model:value="{{testb}}" placeholder="请填写"/>
</view>
<view><button type="button" class="bt1" bindtap="btna">下一步</button></view> -->
 <view class="numberInfo">
 ** 信息录入</view>
 
<view class="container">
 
  <view class="lineHeight" type="number">手机号
     <input class='input' placeholder='请输入手机号'></input>
  </view>
    <view class="lineHeight" type="text">姓名
     <input class='input-15' placeholder='姓名'></input>
  </view>
  <view class="lineHeight" type="text">公司名称
     <input class='input-7' placeholder='公司名称'></input>
  </view>
  <view class="lineHeight">公司电话
      <input class='input-7' type='number' placeholder='区号'></input>
  </view>
  <view class="lineHeight" type='number'>分机号码
      <input class='input-7' placeholder='公司分机号码（选填）'></input>
  </view>
  <view class="lineHeight" type="text">
      <!-- <input class='input-7'></input> -->
    <picker bindchange="bindPickerChange" value="{{index}}" range="{{array}}"  bindtap='clearFont'>
            产品/服务
            <text class='select' >{{placeholder}} {{array[index]}}</text>
    </picker>
  </view>
  <view class="lineHeight" type="text">
      <!-- <input class='input-7' placeholder='请选择'></input> -->
      <view class="section">
        <!-- <view class="section__title">省市区选择器</view> -->
          <picker
             mode="region"
             bindchange="bindRegionChange"
             value="{{region}}"
             custom-item="{{customItem}}"
          >
            <view class="picker">
              公司地址 <text class='select'>{{region[0]}}，{{region[1]}}，{{region[2]}}</text>
            </view>
         </picker>
  </view>
  </view>
  <view class="flex-between form-item">
      <view class="item-form-title">所在地区</view>
      <view class="item-form-input" bindtap="onRegionTap">
          <input model:value="{{area}}" placeholder="请选择" placeholder-class="placeholder"
              disabled="true" />
          <view>
              <image src="/images/gengduo_gonggao@2x.png"></image>
          </view>
      </view>
  </view>
  <view class="lineHeight" type="text">具体地址
      <input class='input-7' placeholder='具体地址'></  input>
  </view>
</view>
 
 <view style='margin-top:100rpx'>日期选择器</view>
 <picker mode="date" start="" end="" value='' fields="day" bindchange='changeDate'>
 {{dataLable}}请选择
 </picker>
 <input value="{{currentDate}}" placeholder='显示当前选择日期' />
 安装位置图片集合
<view class="weui-uploader">
  <view class="img-v weui-uploader__bd">
    <view class='pic' wx:for="{{imgs}}" wx:for-item="item" wx:key="*this">	
		<p>{{item.photo}}</p>
       <image class='weui-uploader img'
                src="{{item.photo}}"
                data-index="{{index}}" mode="aspectFill" bindtap="previewImg">
                  <icon type='cancel' class="delete-btn" data-index="{{index}}" catchtap="deleteImg"></icon>
        </image>
    </view>
     
      <!-- 用来提示用户上传图片 -->
      <view class="weui-uploader__input-box pic" bindtap="chooseImg"> </view>
  </view>

</view>
<!-- <image  src="{{imgtest}}"></image>test -->
企业主体照片
<view class="weui-uploader">
    <view class="img-v weui-uploader__bd"> 
	    <view class='pic' >
	       <image class='weui-uploader img'
	                src="{{imgc}}"
	                mode="aspectFill" bindtap="previewImg">
	                  
	        </image>
	    </view>
        <view class="weui-uploader__input-box pic" bindtap="choosecImg"> </view>
	</view>
</view>
证明照片集合
<view class="weui-uploader">
	<view class='pic' wx:for="{{imgz}}" wx:for-item="item" wx:key="*this">
	    <image class='weui-uploader img'
	            src="{{item.photo}}"
	            data-index="{{index}}" mode="aspectFill" bindtap="previewImg">
	              <icon type='cancel' class="delete-btn" data-index="{{index}}" catchtap="deleteImg"></icon>
	    </image>
	</view>
     <view class="img-v weui-uploader__bd">
         <view class="weui-uploader__input-box pic" bindtap="choosezImg"> </view>
     </view> 
</view>
授权书
<view class="weui-uploader">
    <view class="img-v weui-uploader__bd"> 
	    <view class='pic' >
	        <image class='weui-uploader img'
	                src="{{imgd}}"
	                mode="aspectFill" bindtap="previewImg">
	                  
	        </image>
	    </view>
        <view class="weui-uploader__input-box pic" bindtap="choosedImg"> </view>
	</view>
</view>
法人身份证件照集合
<view class="weui-uploader">
  <view class="img-v weui-uploader__bd">
	  <view class='pic' wx:for="{{imgf}}" wx:for-item="item" wx:key="*this">
	      <image class='weui-uploader img'
	              src="{{item.photo}}"
	              data-index="{{index}}" mode="aspectFill" bindtap="previewImg">
	                <icon type='cancel' class="delete-btn" data-index="{{index}}" catchtap="deleteImg"></icon>
	      </image>
	  </view>
      <view class="weui-uploader__input-box pic" bindtap="choosefImg"> </view>
  </view> 
  <button class="upload-img-btn" bindtap="addForm" type='primary'>上传</button>
</view>


<view class="model-public-c" wx:if="{{showAddressModel}}">
    <view class='address-c animated {{showAddressModel ?"fadeInUp":""}}'>
        <image bindtap="closedModel" class="closed-btn" src="../../images/close.png" />
        <view class="address-title">请选择</view>
        <view class="address-tab">
            <block wx:for="{{areaArray}}">
                <text>{{item}}</text>
            </block>
        </view>
        <scroll-view class="address-list" scroll-y="true" style="height:700rpx;">   
            <block wx:for="{{addressView}}" wx:key="index">
                <view class="address-item" data-id="{{item.id}}" data-parentId="{{item.parentId}}" data-name="{{item.name}}" bindtap="onRegionTap"> 
                    {{item.name}}
                </view>
            </block>
        </scroll-view>
    </view>
</view>

